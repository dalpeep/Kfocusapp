const headers={'Content-Type':'application/json; charset=utf-8','Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'Content-Type, Authorization','Access-Control-Allow-Methods':'POST, OPTIONS'};
const out=(statusCode,body)=>({statusCode,headers,body:JSON.stringify(body)});function textFromResponse(j){return j.output_text||j.output?.flatMap(x=>x.content||[]).find(x=>x.type==='output_text')?.text||'';}function parseJsonText(t=''){const c=String(t).replace(/^```json\s*/i,'').replace(/```$/,'').trim();try{return JSON.parse(c)}catch(_){const a=c.indexOf('{'),b=c.lastIndexOf('}');if(a>=0&&b>a)return JSON.parse(c.slice(a,b+1));throw new Error('AI 기사 결과를 JSON으로 해석하지 못했습니다.')}}
async function sb(path,opt={}){const u=process.env.SUPABASE_URL,k=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!u||!k)throw new Error('Supabase 서비스 환경변수가 없습니다.');const r=await fetch(`${u.replace(/\/$/,'')}/rest/v1/${path}`,{...opt,headers:{apikey:k,Authorization:`Bearer ${k}`,'Content-Type':'application/json',...(opt.headers||{})}});const txt=await r.text();if(!r.ok)throw new Error(txt||`Supabase 오류 ${r.status}`);return txt?JSON.parse(txt):null;}
exports.handler=async event=>{if(event.httpMethod==='OPTIONS')return out(200,{ok:true});try{const b=JSON.parse(event.body||'{}');if(!b.id)throw new Error('기사 ID가 없습니다.');const rows=await sb(`newsroom_items?id=eq.${encodeURIComponent(b.id)}&select=*`),item=rows?.[0];if(!item)throw new Error('기사를 찾지 못했습니다.');if(!process.env.OPENAI_API_KEY)throw new Error('OPENAI_API_KEY가 설정되지 않았습니다.');const prompt=`Write an original Korean local-information article from this verified source record. Do not copy sentences. Preserve exact dates, times, road names and agency names. Keep it concise and useful to Korean readers in DFW. Destination: ${item.destination||item.suggested_destination||'life'}. Title: ${item.original_title}. Source summary: ${item.original_summary||''}. Source: ${item.source_name||''}. URL: ${item.original_url}. AI working title: ${item.ai_title||''}. AI working summary: ${item.ai_summary||''}. Return ONLY JSON {"ai_title":"","ai_summary":"2-3 sentences","ai_content":"article body without source footer"}. Do not invent facts.`;const r=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{Authorization:`Bearer ${process.env.OPENAI_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({model:process.env.NEWSROOM_OPENAI_MODEL||'gpt-5-mini',input:prompt})});const j=await r.json();if(!r.ok)throw new Error(j?.error?.message||`OpenAI 오류 ${r.status}`);const a=parseJsonText(textFromResponse(j));await sb(`newsroom_items?id=eq.${encodeURIComponent(item.id)}`,{method:'PATCH',headers:{Prefer:'return=minimal'},body:JSON.stringify({ai_title:a.ai_title||item.ai_title||item.original_title,ai_summary:a.ai_summary||item.ai_summary||'',ai_content:a.ai_content||'',status:'review',updated_at:new Date().toISOString()})});return out(200,{ok:true});}catch(e){console.error(e);return out(500,{ok:false,error:e.message})}};
