const {json,rest,emailOk,normalizedEmail,code,sendEmail,getCoupon,getBusiness,couponOpen}=require('./coupon-campaign-lib');
exports.handler=async(event)=>{if(event.httpMethod==='OPTIONS')return json(200,{ok:true});if(event.httpMethod!=='POST')return json(405,{ok:false,error:'POST only'});try{const b=JSON.parse(event.body||'{}');const couponId=String(b.coupon_id||'').trim();const email=normalizedEmail(b.email);const marketing=!!b.marketing_opt_in;if(!couponId)return json(400,{ok:false,error:'coupon_id가 필요합니다.'});if(!emailOk(email))return json(400,{ok:false,error:'올바른 이메일 주소를 입력하세요.'});const coupon=await getCoupon(couponId);if(!coupon)return json(404,{ok:false,error:'쿠폰을 찾을 수 없습니다.'});const mode=String(coupon.delivery_mode||'display');if(!['instant_email','raffle'].includes(mode))return json(400,{ok:false,error:'이 쿠폰은 이메일 발급/응모형이 아닙니다.'});const open=couponOpen(coupon);if(!open.ok)return json(409,{ok:false,error:open.reason});const existed=await rest(`coupon_entries?select=*&coupon_id=eq.${encodeURIComponent(couponId)}&email_normalized=eq.${encodeURIComponent(email)}&limit=1`);if(Array.isArray(existed)&&existed[0]){const r=existed[0];return json(200,{ok:true,existing:true,mode,status:r.status,entry_code:r.entry_code||'',coupon_code:r.coupon_code||'',message:mode==='instant_email'?'이미 발급된 쿠폰입니다.':'이미 응모한 이메일입니다.'})}const biz=await getBusiness(coupon.business_id);const bizName=biz?.name_ko||biz?.name||biz?.name_en||'참여 업소';const appUrl=process.env.APP_PUBLIC_URL||'https://daltownmap.com';const now=new Date().toISOString();if(mode==='instant_email'){const couponCode=code('DAL',8);const payload={coupon_id:couponId,business_id:String(coupon.business_id||''),email,email_normalized:email,coupon_code:couponCode,status:'coupon_issued',marketing_opt_in:marketing,source:String(b.source||'app'),issued_at:now,created_at:now,updated_at:now};const inserted=await rest('coupon_entries',{method:'POST',headers:{Prefer:'return=representation'},body:JSON.stringify(payload)});const row=Array.isArray(inserted)?inserted[0]:payload;await sendEmail({to:email,subject:`🎟️ ${coupon.title||'DalTownMap 쿠폰'} 발급 완료`,title:coupon.title||'쿠폰 발급 완료',bodyLines:[`${bizName} 쿠폰이 발급되었습니다.`,coupon.discount_label||coupon.description||'쿠폰 혜택을 확인해 주세요.',coupon.end_at?`사용 종료: ${new Date(coupon.end_at).toLocaleString('ko-KR',{timeZone:'America/Chicago'})}`:''].filter(Boolean),codeLabel:'COUPON CODE',codeValue:couponCode,buttonUrl:appUrl});if(row.id)await rest(`coupon_entries?id=eq.${encodeURIComponent(row.id)}`,{method:'PATCH',headers:{Prefer:'return=minimal'},body:JSON.stringify({emailed_at:new Date().toISOString(),updated_at:new Date().toISOString()})});return json(200,{ok:true,mode,status:'coupon_issued',coupon_code:couponCode,message:'쿠폰을 이메일로 발급했습니다.'})}const entryCode=code('ENTRY',8);const payload={coupon_id:couponId,business_id:String(coupon.business_id||''),email,email_normalized:email,entry_code:entryCode,status:'entered',marketing_opt_in:marketing,source:String(b.source||'app'),created_at:now,updated_at:now};const inserted=await rest('coupon_entries',{method:'POST',headers:{Prefer:'return=representation'},body:JSON.stringify(payload)});const row=Array.isArray(inserted)?inserted[0]:payload;await sendEmail({to:email,subject:`🎫 ${coupon.title||'DalTownMap 이벤트'} 응모 완료`,title:coupon.title||'이벤트 응모 완료',bodyLines:[`${bizName} 이벤트 응모가 완료되었습니다.`,'당첨자는 추첨 후 별도 이메일로 당첨 쿠폰을 받게 됩니다.',coupon.raffle_end_at?`응모 마감: ${new Date(coupon.raffle_end_at).toLocaleString('ko-KR',{timeZone:'America/Chicago'})}`:''].filter(Boolean),codeLabel:'ENTRY CODE',codeValue:entryCode,buttonUrl:appUrl});if(row.id)await rest(`coupon_entries?id=eq.${encodeURIComponent(row.id)}`,{method:'PATCH',headers:{Prefer:'return=minimal'},body:JSON.stringify({emailed_at:new Date().toISOString(),updated_at:new Date().toISOString()})});return json(200,{ok:true,mode,status:'entered',entry_code:entryCode,message:'응모가 완료되었습니다.'})}catch(e){console.error('[coupon-campaign-enter]',e);return json(500,{ok:false,error:e.message||String(e)})}};
