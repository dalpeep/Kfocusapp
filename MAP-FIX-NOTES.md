# Map fix notes

- Show all valid business markers for the selected map category.
- Keep the bottom panel as a nearby-business list based on the current center/radius.
- Move the current-location status badge outside the Google Maps internal DOM so it remains visible after map redraws.
- Restore the current-location marker and status badge when the locate button is pressed.
- Add the missing `suppressMapUiChange` state declaration.
