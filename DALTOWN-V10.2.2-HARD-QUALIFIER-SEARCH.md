# DalTownMap v10.2.2 — 핵심 조건 보존 검색

## 수정 내용
- `한인`, `한국어`, `Korean-speaking`, `여의사`, `소아`, `내과`, `치과` 같은 핵심 조건을 자동 추출합니다.
- Google Places 검색어에 핵심 조건을 강제로 유지합니다.
- 사용자가 Google 검색 링크를 넣으면 URL의 `q` 검색어를 추출해 장소 검색에 반영합니다.
- 장소 후보의 공식 홈페이지를 읽고 핵심 조건의 명시적 증거가 있는 장소만 기사 근거로 사용합니다.
- `한인 병원` 요청인데 한국어 지원·한인 의료진 근거가 없는 일반 병원은 자동 제외합니다.
- 조건을 확인할 수 있는 장소가 없으면 임의 추천하지 않고 생성을 중단합니다.
- Places 검색은 병렬 처리하고 공식 사이트 읽기는 최대 4곳, 페이지 제한 시간은 4.5초로 제한했습니다.

## 환경변수
- `OPENAI_API_KEY`
- `GOOGLE_MAPS_API_KEY`
