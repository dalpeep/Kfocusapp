# DalTownMap v21.6 — DalPick 모바일 스와이프 수정

## 수정 내용

- DalPick 카드에서 좌우로 넘길 때 브라우저 화면 전체가 함께 밀리는 현상 차단
- 손가락 방향을 먼저 판별하여 가로 제스처일 때만 기본 브라우저 동작 차단
- 세로 페이지 스크롤은 정상 유지
- iPhone/Safari의 가로 뒤로가기·오버스크롤 영향을 줄이기 위해 `touch-action` 및 `overscroll-behavior-x` 적용
- 터치 취소 시 자동 슬라이드 재시작

## 적용

ZIP의 `app.js`를 프로젝트 루트의 기존 파일에 덮어쓴 뒤 Netlify에 재배포합니다.
